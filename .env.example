DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_NAME=
DATABASE_USER=
DATABASE_PASS=

# Optional. The values below are the defaults the API applies when the variable is absent.
# INGESTION_MAX_ITEMS=500
# INGESTION_MAX_BYTES=2097152
# INGESTION_TIMEOUT_SECONDS=30
# DATABASE_POOL_MAX_SIZE=5
# DATABASE_POOL_ACQUIRE_TIMEOUT_MS=5000
